Создайте хранилиûе секретов otus/ с Secret Engine KV, а в нем
секрет otus/cred, содержаûий username='otus' password='asajkjkahs’
● В namespace hashicorp-vault-space создайте serviceAccount с именем vault-auth и
ClusterRoleBinding длā него с ролþĀ system:auth-delegator.
Приложите полуùивúиесā манифестý к резулþтатам ДЗ
● В Vault вклĀùите авторизаøиĀ auth/kubernetes и сконфигурируйте
ее исполþзуā токен и сертификат ранее созданного ServiceAccount
● Создайте и примените политику otus-policy длā секретов /otus/cred
с capabilities = [“read”, “list”]. Файл .hcl с политикой приложите к
резулþтатам ДЗ


kubectl exec -n hashicorp-vault-space vault-auth-0 -- sh -c "
  vault secrets enable -path=otus kv
  vault kv put otus/cred username=otus password=asajkjkahs
"